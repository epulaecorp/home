git config --global user.email "epulae.corp@gmail.com"
git config --global user.name "fvicente"

echo "# smartHome" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/epulaecorp/smartHome.git
git push -u origin main